{
  "name": "portfolio-template",
  "version": "1.0.0",
  "description": "A self-hostable portfolio site with an inline, no-code CMS, built with Eleventy.",
  "private": true,
  "scripts": {
    "start": "eleventy --serve",
    "build": "eleventy",
    "clean": "rm -rf _site"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0"
  }
}
